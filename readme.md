# Local k8s/docker files

## k3d-cluster

Convenience files to setup k3d cluster. This installs the cluster without an ingress controller, so that should be installed separately after the cluster setup.